import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.senas133.appmovil',
  appName: 'Señas 133',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
